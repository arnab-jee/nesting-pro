# Update 004

So, should we implement a mechansim to add our stock boards into the system?

Let's build on progressively. Our core system is somewhat ready. What I now want to implement:
- A login system
- A company system with favorites, like:
   - Stock Boards
   - Optimization placement for [Manual(Panel Saw), Nanxing, or others(if any)]
   - Waste Placement defaults
   - For confirm columns, the schema should be named and editable. Currently [Nesting Machine, Panel Saw]. I should be renamed to `Template-1` and `Template`. Default it to `Template-1`.